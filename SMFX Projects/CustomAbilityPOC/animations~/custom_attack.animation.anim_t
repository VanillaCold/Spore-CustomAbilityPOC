length 1

event event0 customability_message -type message -id AnimationManager_Message -flags 0x60
end
#the message AnimationManager_Message triggers the AbilityManager's HandleMessage() function, which triggers custom
channel root root -blendGroup 0
	info
		0 -event event0
	end
end